[project]
name = "codewerk"
version = "0.3.0"
description = "A programmable factory automation game"
requires-python = ">=3.11"
dependencies = ["Pillow>=10.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
